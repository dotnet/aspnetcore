{
    "adx": { // Packages written by the ADX team and that ship on NuGet.org
        "rules": [
            "SigningVerificationCompositeRule"
        ],
        "packages": {
            "Microsoft.VisualStudio.Web.CodeGeneration.Tools": {
                "exclusions": {
                    "BUILD_ITEMS_FRAMEWORK": {
                        "*": "This package intentionally targets .NET Standard 1.0 even though it only carries .NET Core assemblies."
                    },
                    "WRONG_THIRDPARTY_DEPENDENCY_VERSION": {
                        "Microsoft.VisualStudio.Web.CodeGeneration.Tools; .NETCoreApp,Version=v1.0": "This package intentionally depends on version 9.0.1 of Newtonsoft.Json because of the dependency on msbuild(CLI).",
                        "Microsoft.VisualStudio.LanguageServices.Razor; .NETFramework,Version=v4.6": "This package intentionally depends on version 8.0.3 of Newtonsoft.Json because of the dependency on Visual Studio."
                    }
                },
                "packageTypes": [
                    "DotnetCliTool"
                ]
            },
            "Microsoft.VisualStudio.Web.BrowserLink": {
                "Exclusions": {
                    "NUSPEC_TAGS": {
                        "*": "No package verification performed on the repo"
                    }
                }
            }
        }
    },
    "default": { // Rules to run for packages not listed in any other set.
        "rules": [
            "SigningVerificationCompositeRule"
        ],
        "packages": {}
    }
}
