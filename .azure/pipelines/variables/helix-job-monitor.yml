variables:
- name: HelixJobMonitorVersion
  # Keep this in sync with the Microsoft.DotNet.Helix.JobMonitor <Dependency> version in
  # eng/Version.Details.xml. This can't be read directly from that file because AzDO pipeline
  # YAML has no access to MSBuild properties, so it's duplicated here.
  value: 11.0.0-beta.26365.101
