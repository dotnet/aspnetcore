<Project>
  <PropertyGroup>
    <!-- Workaround while there is no SDK available that understands the TFM; suppress unsupported version errors. -->
    <NETCoreAppMaximumVersion>$(AspNetCoreMajorVersion).$(AspNetCoreMinorVersion)</NETCoreAppMaximumVersion>
  </PropertyGroup>
</Project>