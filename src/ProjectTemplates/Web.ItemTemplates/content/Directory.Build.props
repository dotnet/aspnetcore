<Project>
    <!--
        This file intentionally left blank to ensure the template content projects
        are independent from the template package build config.

        The generated template .csproj files in this directory are excluded from build
        via ProjectToExclude in eng/Build.props.
    -->
</Project>
