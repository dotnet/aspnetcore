{
  "name": "client-ts",
  "version": "0.0.0",
  "description": "Provides a root to run npm scripts from. DO NOT PUBLISH",
  "private": true,
  "scripts": {
    "build": "cd ./signalr && npm run build && cd ../signalr-protocol-msgpack && npm run build",
    "test": "npx jest.js --config ./jest.config.js",
    "coverage": "npx jest.js --config ./jest.config.js --coverage"
  },
  "devDependencies": {
    "ts-loader": "^9.2.6"
  },
  "author": "Microsoft",
  "license": "MIT"
}
