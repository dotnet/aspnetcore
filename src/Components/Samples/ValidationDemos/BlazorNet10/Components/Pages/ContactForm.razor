@page "/contact"
@rendermode InteractiveServer
@using System.Globalization
@using Microsoft.Extensions.Localization
@using BlazorNet10.Data
@using BlazorNet10.Resources

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <h1 class="h3 mb-1">@Loc["ContactTitle"]</h1>
            <p class="text-body-secondary mb-4">@Loc["ContactSubtitle"]</p>

            <div class="mb-3 d-flex gap-2 small">
                <span>🌐</span>
                <a href="?culture=en" class="@(CultureInfo.CurrentUICulture.TwoLetterISOLanguageName == "en" ? "fw-bold" : "")">English</a>
                <a href="?culture=es" class="@(CultureInfo.CurrentUICulture.TwoLetterISOLanguageName == "es" ? "fw-bold" : "")">Español</a>
            </div>

            @if (_submitted)
            {
                <div class="alert alert-success">
                    ✅ @Loc["ContactSuccess"]
                </div>
                <button class="btn btn-outline-primary btn-sm" @onclick="ResetForm">@Loc["SubmitAnother"]</button>
            }
            else
            {
                <EditForm EditContext="_editContext" OnValidSubmit="HandleValidSubmit">
                    <DataAnnotationsValidator />
                    <ValidationSummary />

                    <h6 class="text-body-secondary mt-2 mb-3">① @Loc["ApproachNone"]</h6>

                    <div class="mb-3">
                        <label for="Name" class="form-label">Name</label>
                        <InputText @bind-Value="_model.Name" id="Name" class="form-control" />
                        <ValidationMessage For="@(() => _model.Name)" />
                    </div>

                    <h6 class="text-body-secondary mt-4 mb-3">② @Loc["ApproachResourceType"]</h6>

                    <h6 class="text-body-secondary mt-4 mb-3">③ @Loc["ApproachStringLocalizer"]</h6>

                    <div class="mb-3">
                        <label for="Email" class="form-label">@Loc["Email"]</label>
                        <InputText @bind-Value="_model.Email" id="Email" class="form-control" />
                        <ValidationMessage For="@(() => _model.Email)" />
                    </div>

                    <div class="mb-3">
                        <label for="Message" class="form-label">@Loc["Message"]</label>
                        <InputTextArea @bind-Value="_model.Message" id="Message" class="form-control" rows="4" />
                        <ValidationMessage For="@(() => _model.Message)" />
                    </div>

                    <button type="submit" class="btn btn-primary">@Loc["Send"]</button>
                </EditForm>
            }
        </div>
    </div>
</div>

@code {
    [Inject] private IStringLocalizer<LocalizedStrings> Loc { get; set; } = default!;

    private ContactModel _model = new();
    private EditContext _editContext = default!;
    private bool _submitted;

    protected override void OnInitialized()
    {
        _editContext = new EditContext(_model);
    }

    private Task HandleValidSubmit()
    {
        _submitted = true;
        return Task.CompletedTask;
    }

    private void ResetForm()
    {
        _model = new();
        _editContext = new EditContext(_model);
        _submitted = false;
    }
}
