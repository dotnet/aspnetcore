@if (loaded)
{
    <text>Loaded</text>
}
else
{
    <text>Loading...</text>
}
@code {
    bool loaded;
    protected override async Task OnInitializedAsync()
    {
        await Task.Yield();
        loaded = true;
    }
}
