<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../../../Testing/eng/targets/Microsoft.AspNetCore.Components.Testing.props" />

  <PropertyGroup>
    <TargetFramework>$(DefaultNetCoreTargetFramework)</TargetFramework>
    <OutputType>Exe</OutputType>
    <EnableMSTestRunner>true</EnableMSTestRunner>
    <TestRunnerName>Microsoft.Testing.Platform</TestRunnerName>
    <TestCaptureOutput>false</TestCaptureOutput>
    <TestDependsOnPlaywright>true</TestDependsOnPlaywright>
    <TestRunnerAdditionalArguments>--timeout 15m --maximum-failed-tests 25 --minimum-expected-tests 1 --filter "TestCategory!=Quarantined"</TestRunnerAdditionalArguments>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <DisablePackageReferenceRestrictions>true</DisablePackageReferenceRestrictions>
    <GenerateLoggingTestingAssemblyAttributes>false</GenerateLoggingTestingAssemblyAttributes>
    <NoWarn>$(NoWarn);NETSDK1023;MSTEST0001;NU1903</NoWarn>
    <!--
      Publish the app under test instead of launching it with 'dotnet run'. Each test starts
      its own app instance, and concurrent 'dotnet run' invocations rebuild the same project
      and fail on file locks.
    -->
    <E2EAppMode>publish</E2EAppMode>
  </PropertyGroup>

  <ItemGroup>
    <Reference Remove="Microsoft.AspNetCore.InternalTesting" />
    <Reference Remove="Moq" />
    <Compile Remove="$(SharedSourceRoot)test\SuccessfulTests.cs" />
    <Using Remove="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="@(AspNetCoreAppReference)" />
    <Reference Include="@(AspNetCoreAppReferenceAndPackage)" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="Microsoft.Playwright" />
    <Reference Include="Microsoft.Testing.Extensions.TrxReport" />
    <Reference Include="MSTest.TestFramework" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="MSTest.TestAdapter" Version="$(MSTestVersion)" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="Microsoft.Extensions.AI" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="Microsoft.AspNetCore.Components.Testing" />
    <ProjectReference Include="../AIApp/AIApp.csproj">
      <E2EApp>true</E2EApp>
    </ProjectReference>
  </ItemGroup>

  <PropertyGroup>
    <_ImportComponentsTestingTargets>true</_ImportComponentsTestingTargets>
    <_E2ETasksAssembly Condition="'$(_E2ETasksAssembly)' == ''">$(ArtifactsBinDir)Microsoft.AspNetCore.Components.Testing.Tasks\$(Configuration)\netstandard2.0\Microsoft.AspNetCore.Components.Testing.Tasks.dll</_E2ETasksAssembly>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference
      Include="../../../Testing/tasks/Microsoft.AspNetCore.Components.Testing.Tasks.csproj"
      ReferenceOutputAssembly="false"
      SkipGetTargetFrameworkProperties="true"
      UndefineProperties="TargetFramework" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference
      Include="../../../Testing/gen/Microsoft.AspNetCore.Components.Testing.Generators.csproj"
      OutputItemType="Analyzer"
      ReferenceOutputAssembly="false" />
  </ItemGroup>

  <!--
    E2E tests require Playwright/browser infrastructure, so they are not run by Arcade's
    Test target during regular CI builds or on Helix. Clearing IsUnitTestProject (rather
    than IsTestProject) keeps the project classified as a test project, so the public API
    analyzers stay off. They are run explicitly by the components E2E pipeline.
  -->
  <PropertyGroup>
    <IsUnitTestProject>false</IsUnitTestProject>
    <SkipTests>true</SkipTests>
    <BuildHelixPayload>false</BuildHelixPayload>
  </PropertyGroup>

  <ItemGroup>
    <None Include="Baselines\**\*.recording.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <Import Project="../../../Testing/eng/targets/Microsoft.AspNetCore.Components.Testing.targets"
          Condition="'$(_ImportComponentsTestingTargets)' == 'true'" />

</Project>
