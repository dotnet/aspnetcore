@page "/cache-component"
@page "/cache-component/vary"
@using Microsoft.AspNetCore.Components.Forms

<h3>CacheView</h3>

<div id="test-1">
    <CacheView ExpiresAfter="TimeSpan.FromHours(1)" VaryByQuery="testId">
        <p class="cached">@Guid.NewGuid()</p>
        <LiveCachedComponent CssClass="not-cache-component" />
    </CacheView>
    <p class="not-cached">@Guid.NewGuid()</p>
</div>

<div id="test-2">
    <CacheView ExpiresAfter="TimeSpan.FromHours(1)" VaryByQuery="testId" Enabled="false">
        <p class="cached">@Guid.NewGuid()</p>
        <LiveCachedComponent CssClass="not-cache-component" />
    </CacheView>
    <p class="not-cached">@Guid.NewGuid()</p>
</div>

<div id="test-3">
    <EditForm Model="this" FormName="editFormTest" method="post">
        <CacheView ExpiresAfter="TimeSpan.FromHours(1)" VaryByQuery="testId">
            <input id="message-input" type="text" name="Message" />
            <button id="submit" type="submit">Submit</button>
        </CacheView>
        <LiveCachedComponent Id="message" Content="@Message" />
    </EditForm>
</div>

<div id="test-4">
    @for (var i = 0; i < 3; i++)
    {
        <div class="loop-item" data-index="@i">
            <CacheView ExpiresAfter="TimeSpan.FromHours(1)" VaryByQuery="testId" CacheKey="@($"loop-{i}")">
                <p class="cached-value">@Guid.NewGuid()</p>
            </CacheView>
        </div>
    }
</div>

<div id="test-5">
    <CacheView ExpiresAfter="TimeSpan.FromHours(1)" VaryByQuery="testId">
        <p class="cached-content">@Guid.NewGuid()</p>
        @for (var i = 0; i < 2; i++)
        {
            var index = i;
            var label = index == 0 ? "first" : "second";
            <LiveCachedComponent CssClass="@($"live-cached-{index}")" Content="@label" />
        }
    </CacheView>
</div>

<div id="test-7">
    <CacheView ExpiresAfter="TimeSpan.FromHours(1)" VaryByQuery="testId">
        <HardcodedLiveCachedComponentPanel />
    </CacheView>
</div>

<div id="test-8">
    <CacheView ExpiresAfter="TimeSpan.FromHours(1)" VaryByQuery="testId">
        <p class="cached-static">@Guid.NewGuid()</p>
        <StreamingLiveCachedComponent CssClass="streaming-live-cached" />
    </CacheView>
</div>

@code
{
    [SupplyParameterFromForm(FormName = "editFormTest")]
    private string Message { get; set; } = "never";
}
