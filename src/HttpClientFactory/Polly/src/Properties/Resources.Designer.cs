// <auto-generated />
namespace Microsoft.Extensions.Http
{
    using System.Globalization;
    using System.Reflection;
    using System.Resources;

    internal static class Resources
    {
        private static readonly ResourceManager _resourceManager
            = new ResourceManager("Microsoft.Extensions.Http.Resources", typeof(Resources).GetTypeInfo().Assembly);

        /// <summary>
        /// The '{0}' function must return a non-null policy instance. To create a policy that takes no action, use '{1}'.
        /// </summary>
        internal static string PolicyHttpMessageHandler_PolicySelector_ReturnedNull
        {
            get => GetString("PolicyHttpMessageHandler_PolicySelector_ReturnedNull");
        }

        /// <summary>
        /// The '{0}' function must return a non-null policy instance. To create a policy that takes no action, use '{1}'.
        /// </summary>
        internal static string FormatPolicyHttpMessageHandler_PolicySelector_ReturnedNull(object p0, object p1)
            => string.Format(CultureInfo.CurrentCulture, GetString("PolicyHttpMessageHandler_PolicySelector_ReturnedNull"), p0, p1);

        private static string GetString(string name, params string[] formatterNames)
        {
            var value = _resourceManager.GetString(name);

            System.Diagnostics.Debug.Assert(value != null);

            if (formatterNames != null)
            {
                for (var i = 0; i < formatterNames.Length; i++)
                {
                    value = value.Replace("{" + formatterNames[i] + "}", "{" + i + "}");
                }
            }

            return value;
        }
    }
}
