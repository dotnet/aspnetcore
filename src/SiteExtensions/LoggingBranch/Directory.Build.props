<Project>

  <Import Project="$([MSBuild]::GetDirectoryNameOfFileAbove($(MSBuildThisFileDirectory)..\, Directory.Build.props))\Directory.Build.props" />

  <Import Project="..\Sdk\SiteExtension.props" />

  <PropertyGroup>
    <RestoreSources>
        $(RestoreSources);
        $(ArtifactsConfigurationDir)$(SharedFxRid)
    </RestoreSources>
  </PropertyGroup>

</Project>
